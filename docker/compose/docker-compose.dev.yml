# =============================================================================
# AgentID-Chain — Dev Compose (P1.1 PostgreSQL 16)
# =============================================================================
# 本文件在 P1 阶段逐步累积：
#   P1.1            PostgreSQL 16
#   P1.2            Redis 7
#   P1.3            agentid_net 网络
#   P1.4            命名卷
#   P1.5            init-databases.sql 挂载
#   P1.6 [本提交]   mock-chain 占位服务
#   P1.7            完整 dev compose
# =============================================================================

networks:
  agentid_net:
    name: agentid_net
    driver: bridge

services:
  postgres:
    image: postgres:16-alpine
    container_name: dev-postgres
    restart: unless-stopped
    networks:
      - agentid_net
    environment:
      POSTGRES_USER: agentid
      POSTGRES_PASSWORD: agentid_dev
      POSTGRES_DB: agentid
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      # P1.5: 初始化建库脚本（仅在 pg_data 空时执行）
      - ../../scripts/init-databases.sql:/docker-entrypoint-initdb.d/01-init-databases.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentid -d agentid"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: dev-redis
    restart: unless-stopped
    networks:
      - agentid_net
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s

  # P1.6: mock-chain 占位服务
  # 当前使用 alpine:3.19 + 内联 Python HTTP server 提供 :8545 端口 + /healthz
  # 待 P3.8 完成后替换为 agentid-chain/mock-chain:v2.0.1（实现真实 EVM JSON-RPC 子集）
  mock-chain:
    image: alpine:3.19
    container_name: dev-mock-chain
    restart: unless-stopped
    networks:
      - agentid_net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      AGENTID_CHAIN_RPC_URL: "http://mock-chain:8545"
      AGENTID_CHAIN_DRIVER: "mock"
    command:
      - sh
      - -c
      - |
        apk add --no-cache python3 >/dev/null 2>&1
        exec python3 -u -c "
        from http.server import BaseHTTPRequestHandler, HTTPServer
        class H(BaseHTTPRequestHandler):
            def do_GET(self):
                if self.path == '/healthz':
                    self.send_response(200); self.end_headers(); self.wfile.write(b'ok')
                else:
                    self.send_response(200); self.end_headers(); self.wfile.write(b'{\"jsonrpc\":\"2.0\",\"id\":1,\"result\":\"0x0\"}')
            def log_message(self, *a, **k): pass
        HTTPServer(('0.0.0.0', 8545), H).serve_forever()
        "
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:8545/healthz || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 15s

volumes:
  pg_data:
    name: agentid_pg_data
    driver: local
  redis_data:
    name: agentid_redis_data
    driver: local
