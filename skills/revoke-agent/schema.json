{
  "name": "revoke_agent",
  "description": "撤销 Agent（不可逆）",
  "version": "2.0.1",
  "input_schema": {
    "type": "object",
    "properties": {
      "uuid": {"type": "string", "format": "uuid"},
      "reason": {"type": "string", "minLength": 1, "maxLength": 500}
    },
    "required": ["uuid", "reason"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "null"
  },
  "mcp_tool": {"name": "revoke_agent", "method": "tools/call"}
}
